

Drop Table list;

CREATE TABLE list (
    worker_ID varchar(255),
    assignment_ID varchar(255),
    hit_ID varchar(255),
    trial varchar(255),
    task varchar(255),
    trial_index varchar(255),
    controller varchar(255)
);
